server {
    listen 80;
    server_name localhost;
    root __ROOTDIR__;
    index index.html;

    location / {
      try_files $uri =404;
    }

    location /api/v1/ {
      include uwsgi_params;
      uwsgi_pass unix:__ROOTDIR__/rovercode.sock;
    }
}
